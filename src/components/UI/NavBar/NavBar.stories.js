import NavBar from "./NavBar"

export default {
  titls: "NavBar",
  component: NavBar,
}

// There are some errors here compaing about Router

// const Template = (args) => <NavBar {...args} />

// export const BaseNavBar = Template.bind({})
// BaseNavBar.args = {}
